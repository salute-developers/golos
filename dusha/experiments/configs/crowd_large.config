from .base import *

# data
train_manifest_path = base_path / 'crowd_large.jsonl'
val_manifest_path = base_path / 'test' / 'crowd_test.jsonl'

# pretrain
pt_model_path = None

# exp hyperparams
batch_size = 64
epoch_count = 100
learning_rate = 1e-3
optimizer_step = 5
optimizer_gamma = 1
weight_decay = 1e-6
clip_grad = False
