from pathlib import Path

# You should specify `DATASET_PATH` in the variable `base_path`
# - If you want to use own python environment - define an ABSOLUTE path to processed_dataset folder
# - If you want to use proposed docker image - define `DATASET_PATH` relative to `DUSHA_REPOSITORY_PATH`
# (`DUSHA_REPOSITORY_PATH` will be mounted to the docker as `/workspace`).
#
# It is already done for default `DATASET_PATH` = :
base_path = Path('/workspace/data/paper_setups')
